# Provenance

Dennis Wu states that:

- `firefly` is his Git identity;
- he personally designed, implemented, and validated the repository components;
- the code has never submitted a real order; and
- the intended public scope is research/opportunity scanning with simulated execution.

Repository history at the remediation baseline (`50d3d51`) contains 37 commits
authored under `firefly` (across two email identities) and 12 commits authored by
`copilot-swe-agent[bot]`. The 12 agent-authored commits remain in Git history and
are disclosed here. This record does not erase or reassign those commit authors.

The current remediation is prepared through an agent-assisted pull request and
must receive independent human/code review before merge. Ownership, authorship,
and agent assistance are separate from claims that a feature is implemented,
validated, safe, or permitted by a platform.
